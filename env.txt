# ============================================================================
# HELLO WEB_SCANS — Coolify Environment Variables
# ============================================================================
# Paste these into your Coolify app's Environment Variables section.
# Fill in every value marked with <FILL_IN>.
# Lines starting with # are comments — don't paste those.
# ============================================================================

# ── App ─────────────────────────────────────────────────────────────────────
APP_NAME="HELLO WEB_SCANS"
APP_ENV=production
APP_KEY=<FILL_IN>
APP_DEBUG=false
APP_URL=<FILL_IN_YOUR_DOMAIN_WITH_HTTPS>
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12

# ── Logging ─────────────────────────────────────────────────────────────────
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# ── Database (MySQL) ────────────────────────────────────────────────────────
DB_CONNECTION=mysql
DB_HOST=<FILL_IN>
DB_PORT=3306
DB_DATABASE=<FILL_IN>
DB_USERNAME=<FILL_IN>
DB_PASSWORD=<FILL_IN>

# ── Session ─────────────────────────────────────────────────────────────────
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=true

# ── Queue & Cache ───────────────────────────────────────────────────────────
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database

# ── Mail (SMTP) ─────────────────────────────────────────────────────────────
MAIL_MAILER=smtp
MAIL_SCHEME=tls
MAIL_HOST=<FILL_IN>
MAIL_PORT=587
MAIL_USERNAME=<FILL_IN>
MAIL_PASSWORD=<FILL_IN>
MAIL_FROM_ADDRESS=<FILL_IN>
MAIL_FROM_NAME="${APP_NAME}"

# ── AI Providers (fallback keys — users can set their own in settings) ──────
AI_DEFAULT_PROVIDER=gemini
AI_GEMINI_KEY=<FILL_IN_OR_LEAVE_EMPTY>
AI_GEMINI_MODEL=gemini-2.0-flash
AI_OPENAI_KEY=<FILL_IN_OR_LEAVE_EMPTY>
AI_OPENAI_MODEL=gpt-4o
AI_ANTHROPIC_KEY=<FILL_IN_OR_LEAVE_EMPTY>
AI_ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# ── Stripe Billing ──────────────────────────────────────────────────────────
# Get keys from: https://dashboard.stripe.com/apikeys
# Use pk_test_ / sk_test_ for testing, pk_live_ / sk_live_ for production
STRIPE_KEY=<FILL_IN>
STRIPE_SECRET=<FILL_IN>
STRIPE_WEBHOOK_SECRET=<FILL_IN>
CASHIER_CURRENCY=usd
CASHIER_CURRENCY_LOCALE=en

# ── Stripe Price IDs (from Stripe Dashboard → Product catalog) ──────────────
# Leave Free plan empty — free users don't go through Stripe
STRIPE_FREE_MONTHLY_PRICE_ID=
STRIPE_FREE_ANNUAL_PRICE_ID=
STRIPE_PRO_MONTHLY_PRICE_ID=<FILL_IN>
STRIPE_PRO_ANNUAL_PRICE_ID=<FILL_IN>
STRIPE_AGENCY_MONTHLY_PRICE_ID=<FILL_IN>
STRIPE_AGENCY_ANNUAL_PRICE_ID=<FILL_IN>

# ── Vite ────────────────────────────────────────────────────────────────────
VITE_APP_NAME="${APP_NAME}"

# ============================================================================
# NOTES
# ============================================================================
#
# APP_KEY: Generate on the server with: php artisan key:generate --show
#          Then paste the output here (starts with base64:...)
#
# GOOGLE API KEY + ZYTE API KEY: These are managed through the admin panel
#          (Settings page), NOT through env variables.
#
# AFTER FIRST DEPLOY, RUN:
#   php artisan migrate --force
#   php artisan storage:link
#   php artisan config:cache
#   php artisan route:cache
#   php artisan view:cache
#   php artisan queue:restart
#
# QUEUE WORKER: You need a persistent process running:
#   php artisan queue:work --sleep=3 --tries=3 --max-time=3600
#   (Use Coolify's worker or Supervisor to keep it alive)
#
# ============================================================================
