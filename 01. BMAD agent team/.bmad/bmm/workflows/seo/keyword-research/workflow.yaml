# Keyword Research Workflow
name: keyword-research
description: "Keyword discovery and strategy workflow with search intent mapping, competition analysis, and prioritization"
author: "The SEO Guy"

# Critical variables from config
config_source: "{project-root}/.bmad/bmm/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
document_output_language: "{config_source}:document_output_language"
date: system-generated

# Web research is critical for keyword discovery
enable_web_research: true

# Workflow components
installed_path: "{project-root}/.bmad/bmm/workflows/seo/keyword-research"
instructions: "{installed_path}/instructions.md"
template: "{installed_path}/template.md"
validation: "{installed_path}/checklist.md"

# Output configuration
default_output_file: "{output_folder}/keyword-research-{{date}}.md"

standalone: true
